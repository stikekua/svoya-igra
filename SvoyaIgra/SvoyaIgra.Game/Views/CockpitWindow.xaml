<Window x:Class="SvoyaIgra.Game.CockpitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:SvoyaIgra.Game"
        xmlns:viewModels="clr-namespace:SvoyaIgra.Game.ViewModels"
        xmlns:helpers="clr-namespace:SvoyaIgra.Game.ViewModels.Helpers"
        mc:Ignorable="d"
        Title="CockpitWindow" 
        Height="1080"         
        Width="1920"
        WindowState="Maximized"
        WindowStyle="SingleBorderWindow">
    <Window.DataContext>
        <viewModels:GameViewModel/>
    </Window.DataContext>


    <Window.Resources>
        <Style TargetType="Label" >
            
        </Style>
        <Style TargetType="Button" x:Key="WindowButtons" >
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei Light"/>
            <Setter Property="Margin" Value="3,0,3,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="3" 
                                BorderBrush="Black" 
                                BorderThickness="1" 
                                Background="#FF7596ED">
                                <TextBlock  Text="{TemplateBinding Content}" 
                                            FontSize="15" 
                                            TextWrapping="Wrap"
                                            Foreground="Black"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox" x:Key="CheckBoxWindow">
            <Setter Property="VerticalAlignment" Value="Center"/>            
        </Style>
        <Style TargetType="TextBlock" x:Key="DefaultTextBlock">
            <Setter Property="FontFamily" Value="Microsoft YaHei Light"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" x:Name="ThisWindowPanelGrid">
            <StackPanel Orientation="Horizontal">
                <Button Content="Open Playscreen" 
                            Command="{Binding OpenPresentScreenCommand}"
                            Style="{StaticResource WindowButtons}">
                    <Button.Resources>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PlayScreenRunning}" Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding PlayScreenRunning}" Value="False">
                                    <Setter Property="IsEnabled" Value="True" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="Close" 
                            Style="{StaticResource WindowButtons}"
                            Command="{Binding ClosePresentScreenCommand}"
                            IsEnabled="{Binding PlayScreenRunning}"/>

                <Border CornerRadius="3" 
                            BorderThickness="1" 
                            BorderBrush="Black"
                            VerticalAlignment="Center"
                            Height="50"
                            Padding="2">
                    <StackPanel Orientation="Horizontal" >
                        <CheckBox Style="{StaticResource CheckBoxWindow}"
                                  VerticalAlignment="Center"
                                  Command="{Binding MovePresentScreenCommand}"
                                  IsChecked="{Binding PlayScreenLocked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  IsEnabled="{Binding PlayScreenRunning, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Lock Playscreen" 
                                       Style="{StaticResource DefaultTextBlock}"/>
                    </StackPanel>
                </Border>
                <Button Content="Close app"
                        Command="{Binding CloseAppCommand}"
                        IsEnabled="{Binding PlayScreenRunning}"
                        Style="{StaticResource WindowButtons}"
                        >
                </Button>
            </StackPanel>

        </Grid>
        <Grid Grid.Row="1" x:Name="PresentWindowControlGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" x:Name="WindowControlGrid">
                <StackPanel Orientation="Horizontal" 
                            Height="50" 
                            VerticalAlignment="Top"
                            IsEnabled="{Binding PlayScreenRunning}">
                    <RadioButton GroupName="GamePhase" Content="Game Intro" Command="{Binding ChangeGamePhaseCommand}" IsChecked="True">
                        <RadioButton.CommandParameter>
                            <system:Int32>0</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" Content="First round Intro" Command="{Binding ChangeGamePhaseCommand}">
                        <RadioButton.CommandParameter>
                            <system:Int32>1</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" Content="First round" Command="{Binding ChangeGamePhaseCommand}">
                        <RadioButton.CommandParameter>
                            <system:Int32>2</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" 
                                 Content="Second round Intro" 
                                 Command="{Binding ChangeGamePhaseCommand}">
                        <RadioButton.CommandParameter>
                            <system:Int32>3</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" Content="Second round" 
                                 Command="{Binding ChangeGamePhaseCommand}">
                        <RadioButton.CommandParameter>
                            <system:Int32>4</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" Content="Third round Intro" 
                                 Command="{Binding ChangeGamePhaseCommand}">
                        <RadioButton.CommandParameter>
                            <system:Int32>5</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" Content="Third round" 
                                 Command="{Binding ChangeGamePhaseCommand}" >
                        <RadioButton.CommandParameter>
                            <system:Int32>6</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" 
                                 Content="Final round Intro" 
                                 Command="{Binding ChangeGamePhaseCommand}">
                        <RadioButton.CommandParameter>
                            <system:Int32>7</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton GroupName="GamePhase" 
                                 Content="Final round" 
                                 Command="{Binding ChangeGamePhaseCommand}">
                        <RadioButton.CommandParameter>
                            <system:Int32>8</system:Int32>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                </StackPanel>
                

            </Grid>
            <Grid Grid.Column="1" x:Name="GamePhaseControlGrid">
                
            </Grid>
        </Grid>
        <Grid Grid.Row="2" x:Name="QuestionsControlGrid">

        </Grid>


    </Grid>
</Window>
